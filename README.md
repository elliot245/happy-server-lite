# happy-server-lite
Happy Server Lite is a minimal implementation of happy-server, specifically designed for personal use cases. Rewritten in Go, it replaces database storage with in-memory storage and can be deployed on an existing VPS, enabling a zero-additional-cost self-hosted solution.
